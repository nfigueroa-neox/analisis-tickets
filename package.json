{
  "name": "nxsupport-horas",
  "version": "1.0.0",
  "description": "Visor de horas trabajadas - NXSupport",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npx ng build --output-path ../dist --output-hashing all"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-session": "^2.1.0",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "xlsx": "^0.18.5"
  }
}
